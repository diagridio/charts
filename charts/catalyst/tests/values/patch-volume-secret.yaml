agent:
  patch:
    volumes:
      - name: app-secret
        secret:
          secretName: my-app-secret
    volumeMounts:
      - name: app-secret
        mountPath: /etc/secrets
        readOnly: true
