{{- $gomemlimit := include "catalyst.GetGoLangGOMEMLIMITFromResourceLimits" (dict "limits" .Values.agent.resources.limits "golimit" .Values.agent.goSoftLimit) }}
{{- $joinToken := required "join_token is required" .Values.join_token }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agent.name" . }}
  labels:
    {{- include "merge.maps" (dict "base" (fromYaml (include "agent.labels" .)) "overrides" .Values.agent.merge.labels) | nindent 4 }}
spec:
  replicas: {{ .Values.agent.replicaCount }}
  selector:
    matchLabels:
      {{- include "agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/cra-agent-config: {{ include (print $.Template.BasePath "/agent/configmap.yaml") . | sha256sum }}
        dapr.io/app-id: agent
        dapr.io/config: agent
        {{ with .Values.agent.podAnnotations }}
        {{ toYaml . | nindent 8 }}
        {{ end }}
      labels:
        {{- include "agent.selectorLabels" . | nindent 8 }}
    spec:
      restartPolicy: Always
      serviceAccountName: {{ include "agent.serviceAccountName" . }}
      securityContext:
        {{ include "merge.maps" (dict "base" .Values.agent.securityContext "overrides" .Values.agent.merge.securityContext) | nindent 6 }}
      {{ with .Values.global.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{ end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.agent.securityContext | nindent 12 }}
          image: {{ include "catalyst.image" (dict "image" .Values.agent.image "consolidated" .Values.global.consolidated_image "global" .Values.global.image "context" .) }}
          imagePullPolicy: {{ include "catalyst.imagePullPolicy" (dict "image" .Values.agent.image "consolidated" .Values.global.consolidated_image "global" .Values.global.image) }}
          command: ["./agent"]
          args:
          - "--log-level={{ .Values.agent.logLevel }}"
          - "--tls.enabled=false"
          {{- if eq .Values.global.secrets.provider "redis" }}
          - "--secretsprovider=redis"
          - "--secrets_redis.host={{ required ".Values.global.secrets.redis.host entry required when .Values.global.secrets.provider is set to redis" .Values.global.secrets.redis.host }}"
          - "--secrets_redis.password={{ .Values.global.secrets.redis.password }}"
          {{- end }}
          {{- if eq .Values.global.secrets.provider "aws" }}
          - "--secretsprovider=aws.secretmanager"
          - "--secrets_aws_secretmanager.region={{ required ".Values.global.secrets.aws.region entry required when .Values.global.secrets.provider is set to aws" .Values.global.secrets.aws.region }}"
          - "--secrets_aws_secretmanager.access_key={{ .Values.global.secrets.aws.access_key }}"
          - "--secrets_aws_secretmanager.secret_access_key={{ .Values.global.secrets.aws.secret_access_key }}"
          {{- end }}
          {{- if eq .Values.global.secrets.provider "kubernetes" }}
          - "--secretsprovider=kubernetes"
          {{- end }}
          env:
          - name: NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: CONFIG_FILE_DIR
            value: /etc/cra-agent
          {{- if (and $gomemlimit (ne $gomemlimit "0")) }}
          - name: GOMEMLIMIT
            value: {{ $gomemlimit }}
          {{- end }}
          {{- if .Values.agent.extraEnvs }}
          {{- toYaml .Values.agent.extraEnvs | nindent 10 }}
          {{- end }}
          {{- include "inject.patch" (dict "patch" .Values.agent.patch.env "indent" 10) }}
          livenessProbe:
            httpGet:
              path: /livez
              port: probes
            failureThreshold: 3
            periodSeconds: 10
            initialDelaySeconds: 5
          ports:
          - name: metrics
            containerPort: {{ .Values.agent.metrics.port }}
            protocol: TCP
          - name: probes
            containerPort: 8080
            protocol: TCP
          {{- if .Values.agent.patch.ports }}
          {{- include "inject.patch" (dict "patch" .Values.agent.patch.ports "indent" 10) }}
          {{- end }}
          resources:
            {{- if .Values.agent.merge.resources }}
              {{- toYaml .Values.agent.merge.resources | nindent 12 }}
            {{- else }}
              {{- toYaml .Values.agent.resources | nindent 12 }}
            {{- end }}
          volumeMounts:
          - name: agent-config
            mountPath: /etc/cra-agent
          - name: sentry-identity-token
            mountPath: /var/run/secrets/dapr.io/sentrytoken
            readOnly: true
          {{- if or .Values.global.charts.clientCert .Values.global.charts.clientKey .Values.global.charts.customCA .Values.global.charts.existingSecret }}
          - name: charts-secret
            mountPath: /var/run/secrets/charts
            readOnly: true
          {{- end }}
          {{- include "inject.patch" (dict "patch" .Values.agent.patch.volumeMounts "indent" 10) }}
        {{- include "inject.patch" (dict "patch" .Values.agent.patch.containers "indent" 6) }}
      volumes:
      - name: agent-config
        configMap:
          name: {{ include "agent.name" . }}-config
      - name: sentry-identity-token
        projected:
          defaultMode: 420
          sources:
          - serviceAccountToken:
              audience: dapr.io/sentry
              expirationSeconds: 7200
              path: token
      {{- if or .Values.global.charts.clientCert .Values.global.charts.clientKey .Values.global.charts.customCA .Values.global.charts.existingSecret }}
      - name: charts-secret
        secret:
          secretName: {{ include "agent.chartsSecretName" . }}
          defaultMode: 0400
      {{- end }}
      {{- include "inject.patch" (dict "patch" .Values.agent.patch.volumes "indent" 6) }}
      nodeSelector:
        {{- include "merge.maps" (dict "base" .Values.agent.nodeSelector "overrides" .Values.agent.merge.nodeSelector) | nindent 8 }}
      affinity:
        {{- include "merge.maps" (dict "base" .Values.agent.affinity "overrides" .Values.agent.merge.affinity) | nindent 8 }}
      tolerations:
        {{- if or .Values.agent.tolerations .Values.agent.merge.tolerations }}
        {{- if .Values.agent.tolerations }}
        {{- toYaml .Values.agent.tolerations | nindent 8 }}
        {{- end }}
        {{- if .Values.agent.merge.tolerations }}
        {{- toYaml .Values.agent.merge.tolerations | nindent 8 }}
        {{- end }}
        {{- else }}
        []
        {{- end }}

